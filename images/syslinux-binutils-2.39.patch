--- a/bios/core/Makefile
+++ b/bios/core/Makefile
@@ -164,7 +164,7 @@ LDSCRIPT = $(SRC)/$(ARCH)/syslinux.ld

 %.elf: %.o $(LIBDEP) $(LDSCRIPT) $(AUXLIBS)
 	$(LD) $(LDFLAGS) -Bsymbolic -pie -E --hash-style=gnu -T $(LDSCRIPT) -M -o $@ $< \
-		--start-group $(LIBS) --whole-archive $(AUXLIBS) --end-group \
+		--start-group $(LIBS) --whole-archive $(AUXLIBS) --end-group --no-dynamic-linker --no-warn-rwx-segments \
 		> $(@:.elf=.map)
 	$(OBJCOPY) --strip-debug --strip-unneeded $@ $@.tmp
 	$(OBJCOPY) --add-gnu-debuglink=$@ $@.tmp

